name: Deploy Backend to Azure

on:
  push:
    branches: [main]
    paths:
      - "backend/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18.x"

      - name: Install dependencies
        run: |
          cd backend
          npm install

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: "swiftApi" #this is the name of the app service in azure
          publish-profile: ${{ secrets.AZURE_APP_SERVICE_PUBLISH_PROFILE2 }} #this is the name of the app service in azure
          package: backend # this is the name of the package to deploy
