name: Deploy Rider App to Azure Static Web Apps

on:
  push:
    branches: [ main ]
    paths:
      - 'rider-app/**'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          
      - name: Build and Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_RIDER }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/rider-app"
          api_location: ""
          output_location: "dist"
        env:
          VITE_API_URL: 'https://swiftlogistics-backend.azurewebsites.net' # Placeholder for production backend
